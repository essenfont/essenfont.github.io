# 13 — About + Subfonts + Docs index

Static content pages.

## Sources

- `src/pages/AboutPage.vue` — `/about`
- `src/pages/SubfontsPage.vue` — `/subfonts`
- `src/pages/docs/` — directory of docs pages

## Implementation

Each becomes a single `.astro` file. For docs, consider MDX (via content collections like changelog).

## Acceptance

- All static pages render
- Docs TOC + navigation preserved
